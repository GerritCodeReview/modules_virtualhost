[access]
	inheritFrom = Public-Projects
[access "refs/*"]
	owner = group modules-virtualhost
[project]
	description = Module to split the projects' space into virtual hosts similarly to an HTTP server and different domain names.
