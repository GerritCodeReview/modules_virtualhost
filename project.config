[access]
	inheritFrom = Public-Projects
[access "refs/*"]
	owner = group modules-virtualhost
[project]
	description = Module to split the projects' space into virtual hosts\n\
similarly of what you would do with an HTTP Server and different\n\
domain names.
